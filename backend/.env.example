# LifeLine AI Backend Environment Configuration

# Server Port (Render automatically injects PORT in production)
PORT=3001

# Optional: Production Frontend URL for restricted CORS (e.g., https://lifeline-ai.vercel.app)
# If omitted or left empty, CORS defaults to allowing all origins ('*').
FRONTEND_URL=
